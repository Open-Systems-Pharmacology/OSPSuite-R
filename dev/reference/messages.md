# List of functions and strings used to signal error messages Extends the `messages` list from ospsuite.utils

List of functions and strings used to signal error messages Extends the
`messages` list from ospsuite.utils

## Usage

``` r
messages
```

## Format

An object of class `list` of length 70.
