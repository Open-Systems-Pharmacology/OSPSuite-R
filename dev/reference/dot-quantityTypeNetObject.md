# Convert a `MoleculeType` integer flag value to the underlying `OSPSuite.Core.Domain.QuantityType` enum object that the engine accepts as a parameter.

Convert a `MoleculeType` integer flag value to the underlying
`OSPSuite.Core.Domain.QuantityType` enum object that the engine accepts
as a parameter.

## Usage

``` r
.quantityTypeNetObject(moleculeType)
```
