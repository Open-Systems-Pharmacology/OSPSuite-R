# Returns the expanded path for `path` and ensure that encoding is applied properly

Returns the expanded path for `path` and ensure that encoding is applied
properly

## Usage

``` r
.expandPath(path)
```

## Arguments

- path:

  Path to expand
