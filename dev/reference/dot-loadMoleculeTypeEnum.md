# Build the `MoleculeType` enum by reading flag values from the underlying `OSPSuite.Core.Domain.QuantityType` enum and keeping only molecule-relevant members.

Build the `MoleculeType` enum by reading flag values from the underlying
`OSPSuite.Core.Domain.QuantityType` enum and keeping only
molecule-relevant members.

## Usage

``` r
.loadMoleculeTypeEnum()
```
