# .getNetTask

Get an instance of the specified `.NET` Task

## Usage

``` r
.getNetTask(taskName)
```

## Arguments

- taskName:

  The name of the task to retrieve (**without** `Get` prefix).

## Value

returns a new instance of of the specified `.NET` task.
