# Merge Behaviors

Merge behaviors available for modules in MoBi. Loaded at package init
from `OSPSuite.Core.Domain.MergeBehavior`. Used by
`MoBiModule$mergeBehavior`.

## Usage

``` r
MergeBehavior
```
