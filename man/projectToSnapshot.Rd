% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities-snapshots.R
\name{projectToSnapshot}
\alias{projectToSnapshot}
\title{Convert project files to snapshots}
\usage{
projectToSnapshot(..., output = ".", application = NULL)
}
\arguments{
\item{...}{character strings, path to project files (\code{.pksim5} or \code{.mbp3}) or
a directory containing project files to convert.}

\item{output}{character string, path to the output directory where to write
the converted snapshot files.}

\item{application}{character string, \code{"PK-Sim"} or \code{"MoBi"}, the application
the project files belong to. If \code{NULL} (default), it is detected for each
project separately.}
}
\description{
Converts one or more project files into snapshot files (\code{.json}) and writes
them to an output directory.

PK-Sim (\code{.pksim5}) and MoBi (\code{.mbp3}) projects are both supported. The
application is detected from the file extension; use \code{application} to force
the choice.
}
\examples{
\dontrun{
projectToSnapshot("path/to/project.pksim5", output = "path/to/output")
projectToSnapshot("path/to/project.mbp3", output = "path/to/output")
}
}
