% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities-data-combined.R
\name{addResidualColumn}
\alias{addResidualColumn}
\title{Add a residual column to paired observed/predicted data}
\usage{
addResidualColumn(
  pairedData,
  observed = "yValuesObserved",
  predicted = "yValuesSimulated",
  residuals = "residualValues",
  scaling = "log"
)
}
\arguments{
\item{pairedData}{A \code{data.frame} or \code{data.table} containing paired observed
and predicted values. The columns specified by \code{observed} and \code{predicted}
must exist.}

\item{observed}{A string specifying the column name for observed values.
Default is \code{"yValuesObserved"}.}

\item{predicted}{A string specifying the column name for predicted
(simulated) values. Default is \code{"yValuesSimulated"}.}

\item{residuals}{A string specifying the name for the new residuals column
to be added. Default is \code{"residualValues"}.}

\item{scaling}{A character string specifying the scaling method.  One of
\code{"log"} (default), \code{"linear"} (or \code{"lin"} / \code{"identity"}), or \code{"ratio"}.}
}
\value{
The input \code{pairedData} with an additional column named according to
the \code{residuals} argument.  The column carries a \code{"label"} attribute
describing how residuals were computed (e.g.
\code{"residuals\\nlog(predicted) - log(observed)"}), which can be used as a
y-axis label in plots.
}
\description{
Calculates residuals between observed and predicted values and adds them as
a new column to the provided data frame or data table. The calculation
supports three scaling methods: \code{"log"}, \code{"linear"} (alias \code{"lin"}), and
\code{"ratio"}.
}
\details{
\subsection{Scaling Methods}{
\itemize{
\item \strong{\code{"linear"} / \code{"lin"}}: Absolute residuals as
\eqn{residual = predicted - observed}.
\item \strong{\code{"log"}}: Log-scale residuals as
\eqn{residual = \log(predicted) - \log(observed)}.
Data points where the observed or predicted value is zero or negative
produce undefined logarithms; these are set to \code{NaN} and a warning is
issued reporting the number of such points.
\item \strong{\code{"ratio"}}: Ratio of observed to predicted as
\eqn{residual = observed / predicted}.
A warning is issued when predicted values are zero or negative.
}
}
}
\examples{
\dontrun{
paired <- data.frame(
  yValuesObserved  = c(1, 2, 4),
  yValuesSimulated = c(1.1, 1.9, 3.8)
)
addResidualColumn(paired, scaling = "log")
addResidualColumn(paired, scaling = "linear")
addResidualColumn(paired, scaling = "ratio")
}

}
\seealso{
Other data-combined: 
\code{\link{DataCombined}},
\code{\link{calculateResiduals}()},
\code{\link{convertUnits}()}
}
\concept{data-combined}
