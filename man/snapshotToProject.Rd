% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities-snapshots.R
\name{snapshotToProject}
\alias{snapshotToProject}
\title{Convert snapshots to project files}
\usage{
snapshotToProject(
  ...,
  output = ".",
  runSimulations = FALSE,
  application = NULL
)
}
\arguments{
\item{...}{character strings, path to snapshot files (\code{.json}) or a directory
containing snapshot files to convert.}

\item{output}{character string, path to the output directory where to write
the converted project files.}

\item{runSimulations}{logical, whether to run the simulations during
conversion (default = \code{FALSE}).}

\item{application}{character string, \code{"PK-Sim"} or \code{"MoBi"}, the application
whose project files are written. If \code{NULL} (default), it is detected for
each snapshot separately.}
}
\description{
Converts one or more snapshot files (\code{.json}) into project files and writes
them to an output directory.

PK-Sim (\code{.pksim5}) and MoBi (\code{.mbp3}) projects are both supported. The
application that wrote a snapshot is detected from the snapshot itself; use
\code{application} to force the choice.
}
\examples{
\dontrun{
snapshotToProject("path/to/snapshot.json", output = "path/to/output")
}
}
