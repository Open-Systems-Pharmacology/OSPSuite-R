# Mapping of string representation for the error types supported by DataSet to the values supported in the importer configuration

Mapping of string representation for the error types supported by
DataSet to the values supported in the importer configuration

## Usage

``` r
.ImporterErrorTypeToDataSetErrorType
```
