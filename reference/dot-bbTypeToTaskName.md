# Mapping from a `BuildingBlockTypes` value to the name of the MoBi task that implements `LoadFromPKML` for that type.

Mapping from a `BuildingBlockTypes` value to the name of the MoBi task
that implements `LoadFromPKML` for that type.

## Usage

``` r
.bbTypeToTaskName(type)
```
