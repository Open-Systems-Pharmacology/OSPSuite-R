# Returns the value of property named `propertyName` from .NET object instance `netObject`

Returns the value of property named `propertyName` from .NET object
instance `netObject`

## Usage

``` r
.getPropertyValue(netObject, propertyName)
```

## Arguments

- netObject:

  .NET object

- propertyName:

  Property name that should be retrieved from the `netObject`
